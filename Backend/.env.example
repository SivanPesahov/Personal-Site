# Flask
FLASK_ENV=development
FLASK_DEBUG=1
SECRET_KEY=replace-me

# Origins של הפרונט (אפשר כמה, מופרדים בפסיק)
CORS_ORIGINS=http://localhost:3000

# MySQL URL (נגדיר מאוחר יותר בשלב ה-DB)
# פורמט: mysql+pymysql://USER:PASSWORD@HOST:3306/DBNAME?charset=utf8mb4
DATABASE_URL=
# דוגמה:
# DATABASE_URL=mysql+pymysql://root:password@127.0.0.1:3306/personal_site?charset=utf8mb4

# Rate limit ברירת מחדל (נשתמש בהמשך)
RATELIMIT_DEFAULT=100/hour